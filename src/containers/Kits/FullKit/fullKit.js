import React from "react";
import classes from "./FullKit.module.css";

const BasicKit = (props) => {
    // const { onClick } = props;

    return (
        <div className={classes.container}>
            <p>acá irá el full kit</p>
        </div>
    );
};

export default BasicKit;
